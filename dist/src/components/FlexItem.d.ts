import React from 'react';
import type { FlexItemProps } from '../index';
declare function FlexItem({ order, grow, shrink, basis, flex, alignSelfAuto, alignSelfStart, alignSelfEnd, alignSelfCenter, alignSelfBaseline, alignSelfStretch, alignSelf, style, children, ...rest }: FlexItemProps): React.ReactElement;
export default FlexItem;
